<h1>id: <%= @message.id %> Edit Page</h1>

<%= form_for(@message) do |f| %>
  <%= f.label :content, 'Message' %>
  <%= f.text_field :content %>
  <%= f.submit 'Submit' %>
<% end %>

<%= link_to 'Move to a list', messages_path %>